# Universal collaborator messaging

## Scope

**Unreleased, current-schema-only implementation.** The user explicitly rejected old-state migrations, dual messaging registrars and legacy delivery fallback. Unsupported Runtime state must fail without being rewritten or deleted. Existing workspaces and other user data are not disposable.

One actual MCP interface and one [shared skill](../../skills/collaborator-messaging/SKILL.md) serve Pi, Claude Code and Codex. Runtime owns authority, mail and evidence; Herdr owns interactive processes. Pi uses a real stdio MCP child, not direct in-process messaging handlers. Development and installed Pi are 0.85.1. Implementation, tests, commits and pushes are authorized; installation/rollout and unrelated collaborator actions remain separate. One explicitly approved [local upgrade and live peer-replacement check](MCP-LIVE-PI.md#follow-up-upgraded-local-runtime-and-peer-replacement) is now recorded; broader rollout remains a separate gate.

## Current implementation

- `service/messaging.ts`: restricted descriptor authority, exact holder/client/terminal/configuration checks, before/after-await fencing.
- `service/state.ts`: current state version **12 only**, atomic publication/operation receipts, immutable recipient binding, separate reference offers, retrieval offers and client receipts. No v1–v11 migration. Ordinary mail cannot carry native claim/admission evidence.
- `mcp/main.mjs` and `mcp/tools.ts`: one stdio endpoint and schema catalog. No lifecycle passthrough, sampling, resources or task tools.
- `mcp/client.ts` and `mcp/pi.ts`: actual initialize/list/call transport, Pi error semantics, exact session/file/cwd preflight, bounded shutdown. The default Runtime entry uses this single registrar, provisions descriptors through trusted registration for already-held identities, and closes its MCP child before unregistering. Tree/compaction epochs fence in-flight results. The explicit descriptor wrapper is now a development test fixture only.

The direct Pi batch/status messaging registrar and its handlers are removed, with no argument translation. Ordinary-mail full-body native delivery and automatic native prompt injection are removed. Monitor and supported task claims remain separate. Pi self-reference offering is implemented as a best-effort hint, not reliable delivery. Native provider provisioning and Pi persisted-body admission remain incomplete.

## Shared tools

| Tool | Responsibility |
|---|---|
| `collaborator_peers` | Up to 12 peers per page, caller identity, namespace and expiry; Pi also gets exact session/file/cwd binding. |
| `collaborator_send` | Explicit recipient, namespace, durable operation ID and bounded body; atomic publication receipt. |
| `collaborator_receive` | **One exact event ID** from a mailbox reference. Persist/reuse an offer before returning the complete ordinary-mail body. No native claim or ACK. |
| `collaborator_received` | Exact namespace/event/token client receipt. Idempotent; does not delete mail or acknowledge native admission. |
| `collaborator_reply` | Exact offered event/token, operation ID and body. Runtime derives the original sender and atomically records receipt and correlated publication. |
| `collaborator_status` | Sender operation lookup, retained event/native delivery evidence, and separate offer/client-receipt timestamps. |

Receive deliberately has no inbox-scan/pagination framework: callers use exact event references. Peer pagination is independent. Ordinary replies must not reuse typed `task.result`. Lifecycle/workspace/task features require deliberate integration, not accidental deletion or duplicate MCP implementations.

## Authority and history

A trusted controller verifies/registers the actual target and held participant, then issues a private descriptor. MCP cannot acquire identity or obtain broad registration keys. Deterministic descriptor paths may be configured before issuance; metadata negotiation grants no authority. Never put credentials in prompts, tool arguments, Git or review packets.

A namespace permanently binds participant, holder generation, target, client, terminal and configuration. Fresh ordinary-mail publication snapshots the unique eligible recipient namespace. The common mailbox representation can record **unbound**, but fresh MCP send/reply rejects absent or ambiguous recipient authority atomically, without publication. Never infer a recipient later. Already-committed retries preserve their original snapshot, including existing unbound records. New holders/clients/namespaces do not inherit old or pre-issuance mail.

An offer is namespace-owned and binds exact event/token/first-offered time. Repeat receive reuses it. Explicit receipt adds first-received time without touching native claims. Fresh reply requires the exact offer/token and still-valid original MCP publication authority; it never redirects to a successor. Already-committed identical reply retries recover the original receipt before checking current recipient availability. Reply fingerprints include event, token and body; changed send/reply input conflicts.

## Pi reference-only notifications

The trusted `messaging.reference` RPC is **not an MCP model tool**. It accepts the exact Pi registration, local participant/generation snapshot and a fresh attempt ID. Runtime requires the same sole active namespace and exact self-registration before and after host verification, then atomically persists a separate event-keyed reference record before returning only namespace/event/attempt IDs. A committed namespace-scoped attempt returns `acquired:false` before another event can be selected. Each event is offered at most once; mail already offered through MCP is skipped. Empty polls do not create attempt tombstones: `acquired:false` is not a durable publication receipt. Pi never automatically retries an empty or ambiguous call.

Pi polls independently of the native inbox, with single-flight heartbeat and session/tree/compaction/shutdown fencing. Both before and after the request, it requires an unchanged held identity, an enabled `collaborator_receive` tool, idle state, no pending messages, and a known empty UI editor. Only `ctx.mode === "tui"` qualifies. RPC has `hasUI:true` but a synthetic empty `getEditorText()` in Pi 0.85.1; it is not editor authority and skips automatic hints, as do other headless/unknown-editor contexts. A distinct custom-message type carries only the reference; it cannot enter native ACK or admission restoration. Reference errors do not clear a healthy registration. A crash, navigation, new human input or lost response after the service commit can permanently lose the hint. This deliberate safety-first slice has **no reliable-delivery guarantee** and never infers SDK submission, persistence, body receipt or provider admission from `sendMessage()` or hooks.

## Bounds and persistence

- Body: 16 KiB UTF-8; receive returns one complete event without truncation.
- Canonical internal base64 preserves the 64 KiB Runtime request ceiling. Messaging responses allow 128 KiB; MCP frames allow 256 KiB including text/structured duplication. The ordinary native client's default remains 64 KiB.
- Grants, publication receipts, body offers and reference offers share the 10,000-record cap; state is capped at 8 MiB. Repeated offers reuse records; capacity failures cannot report successful new offers/publications.
- Ordinary-mail retention does not use native ACK. Publication receipts, retrieval offers and reference offers protect bodies; age-based cleanup requires an expired recipient namespace (or an unbound event) and no retained protection. Namespace expiry clears body/reference offers and receipts and retains a terminal tombstone. Retention may expire namespaces already older than its cutoff; clock rollback cannot resurrect them.
- Post-rename write uncertainty fences reads and mutations until disk recovery and directory sync. Never restore an earlier snapshot over an uncertain commit.
- Pi transport bounds pending requests (12), queued bytes, stderr and request duration (10 seconds). Shutdown closes stdin, escalates the exact child at 100 ms/1 second and awaits close before replacement. Cancellation is not proof of non-publication; no automatic mutation replay.
- Descriptor renewal/repair remains fail-closed until exact old-process quiescence and unresolved operation identities are accounted for.

## Remaining implementation gates

1. **Native provider provisioning:** default Pi messaging registration is now shared MCP; complete native MCP configuration through the existing verified launch/registration path, without custom launcher/proxy scripts or global configuration rewrites. The user approved normal native configuration, hooks and standard permission prompts for live collaborators, with guarded read-only behavior kept separate. Do not silently equate those policies or bypass permissions. Ordinary full-body native delivery is removed; no old argument translation or `legacy_push` mode.
2. **Reference wakes — Pi first:** after the Herdr source roundtable, the user chose to continue Pi reference delivery and **block Claude/Codex automatic wakes**. Installed Herdr 0.9.0/protocol22 and inspected upstream `90e947a` have no authoritative editor-empty signal or queued process-incarnation binding. Existing paste/delayed-Enter sequencing postpones human cancellation; API prechecks cannot make it safe. Do not ship superficial guards or fall back to ordinary PTY injection. The local `/home/deevs/agents/herdr` feature checkout is unchanged and uninstalled. Partition ordinary MCP mail from native Monitor admission and separately supported tasks. The implemented Pi self-delivery persists an exact namespace/client/holder/registration reference attempt before exposing a hint; an offered reference is not proof that Pi submitted, persisted or consumed it. Lost/ambiguous attempts must not automatically replay. No second `inbox.claim` consumer, full-body fallback or fake ACK. Native provider admission and daemon-owned handoff remain separately blocked follow-ups.
3. **Pi persisted-body evidence:** [copied live transcripts](MCP-LIVE-PI.md#follow-up-copied-transcript-history-proof) now pass real Pi RPC restart/reload/navigation and real-model compaction checks. Exact records remain stored; branching excludes them from active ancestry, and compaction can exclude them from active messages while retaining ancestry. Runtime authority was not loaded in that history-only proof. Subsequent local live checks cover explicit controller reload/reacquisition, controlled peer replacement with fresh messaging, stale credential denial and one observed controller compaction overlap. A later [Runtime-enabled same-session lifecycle proof](MCP-LIVE-PI.md#follow-up-runtime-enabled-same-session-lifecycle) passed five exchanges across normal reload renewal, navigation away/restoration and real-model compaction, with exact native results and branch/context checks. Orphaned/expired descriptor repair, crash recovery and uncertain-operation interleavings remain unproven; successful normal renewal does not settle those failure cases. Pi 0.85.1 dispatches message hooks before append and updates memory before disk writes; neither hooks nor `getEntries()` certify a disk commit. Reload is not a disk reread. Read-only inspection of complete session JSONL records must not become an external writer, and readable bytes alone do not prove fsync-based crash durability. Do not label client receipt `admitted`; block release if the required proof is unavailable.
4. **Isolated live ring:** one [real-model Pi-to-Pi ping/reply exchange](MCP-LIVE-PI.md), including automatic TUI references and MCP body retrieval, has passed. Pi → Claude → Codex → Pi and broader proactive human-driven mail remain. Use existing Runtime/native tools, one visible provider session each, actual MCP calls, exact correlation, scoped permission/secret checks and no unsolicited focus changes. Honor the user's approved normal configuration/hooks and permission prompts rather than claiming strict isolation for that mode. No automatic native wake or custom launcher is authorized. Exact process quiescence precedes private auth cleanup.
5. **Daemon/release:** delivery survives initiating Pi exit; handoff/restart does not duplicate or falsely settle attempts. Validate every directed pairing, publish current setup documentation, and obtain explicit rollout approval.

## Native bootstrap candidate

The current candidate configures normal native `workspace-write` through existing `herdr agent start`: inline Claude MCP configuration or Codex server overrides, one package-owned endpoint and the shared skill. No launcher/proxy or automatic native input is added. Fresh interactive confirmation is mandatory even in Auto. Guarded read-only remains separate and does not receive automatic MCP provisioning; normal mode can retain other user tools, MCP servers and hooks, so this is not a six-effective-tools or edit-only boundary.

Issuance follows verified registration and exact live held identity. Native control metadata is now secret-free version 2, backed by immutable owner-private bound credential files; old token-bearing entries are rejected without migration. Consumed-register conflicts can use the original exact reconnect authority, and issuance-response loss retries the same identity. The descriptor issuance fix retains a descriptor after uncertain state commit, while definite pre-rename failure removes the uncommitted file. These are bounded recovery slices, not general repair or safe indefinite human-prompt continuation.

Full validation passes with `env -u PI_PACKAGE_DIR VITEST_MAX_WORKERS=1 npm run check`: **601 tests across 51 files**, lint/typecheck, RPC/print/JSONL mode smokes, zero audit findings and package dry-run. Default-parallel validation previously timed out in Vitest; the supported worker cap avoids that observed stall without changing repository configuration, but its root cause is not established. A frozen implementation review returned Ship with no substantiated findings, using scoped exact-revision diffs after whole-diff retrieval failed. This is code readiness and claims consistency, not rollout approval or raw-transcript certification. Coordinator tests use synthetic Herdr/Runtime responses; they do not certify native CLI permission semantics, real process identity, delivery or the ring. Existing Codex `mcp get` independently accepted the generated TOML stdio configuration shape without starting a model; Claude help documents the selected inline-config/system-context flags.

The failed `YyhZLq` native prerequisite probe retained an isolated OAuth copy because its initial PID capture was missing. A later scoped inspection found no matching processes and the exact tab absent. The user explicitly approved deleting only that copy with the gap stated; removal and unchanged global-auth identity were recorded privately. This resolves the retained copy by user adjudication, not by retroactively certifying the original cleanup.

## Validation

Use existing Vitest and `env -u PI_PACKAGE_DIR npm run check`; no extra test framework or detached commands. Focused cases cover exact binding, worst-case escaped bodies, repeat offers, response loss after offer/reply commit, atomic receipt/reply, changed retries, expired/replaced authority, capacity/retention, unsupported-state rejection and native claim separation. Actual Pi CLI/RPC tests inspect native persisted tool results; their deterministic model and Herdr identity fixtures are **not** a hosted-model live proof or durable-admission certificate. Review meaningful frozen slices independently.

Historical receive/reply service-slice validation: **515 tests across 49 files**, lint/typecheck, RPC/print/JSONL smokes, zero audit findings and package dry-run. Four selected process cases also passed against the installed Pi 0.85.1 binary, including native receive-result persistence. Actual filesystem Monitor events remain claimable natively and are rejected by MCP receive. Capacity, retention, pre-rename write failure and dropped committed response cases are covered. The escaped native-claim fixture explicitly uses a larger inspection client; production native response limits were not widened.

Default Pi registrar validation: **517 tests across 49 files**, full check and five installed-Pi 0.85.1 process cases pass. A fresh static review cleared this slice, including captured-registration provisioning and its shutdown-race regression. The actual default-entry test verifies registration, private issuance, MCP send/status, old-argument rejection and secret-free native transcript. Reference wakes and durable body admission are not part of that verdict.

Native-exclusion validation: **526 tests across 49 files**, full check (including mode smokes, audit and package dry-run), and five installed-Pi process cases pass. A completed independent current-source review returned CLEAR with a minor retention-coverage suggestion; staggered sender/recipient expiry and the unbound age boundary now have additional passing regressions, including dedupe and restart validity. The reviewer could not inspect the uncommitted diff through its diff tool; this is scoped source review, not a verified diff-only verdict. Pi reference offering is not covered by that historical verdict.

Pi-reference validation: **559 tests across 49 files**, full check (type/lint, mode smokes, clean audit and package dry-run), and five installed-Pi 0.85.1 process cases pass. The updated default RPC process case enables receive, receives incoming mail and observes heartbeat verification without acquiring or rendering a reference: RPC's synthetic editor is not trusted. A completed independent exact-snapshot implementation review returned CLEAR with coverage nits; the added boundary cases and final TUI-only delta received a separate completed CLEAR review. That automated-test/review checkpoint did not establish positive actual-TUI notification, hosted-model live behavior or fsync-based body admission.

Subsequent [live Pi-to-Pi proof](MCP-LIVE-PI.md): two real Pi 0.85.1 TUIs using the user's configured `openai-codex/gpt-6-astra` model completed send → automatic reference → MCP receive → correlated reply → automatic reference → receive/client receipt. Actual Herdr verification and MCP subprocesses were used; no mocked host or model. Exactly two messages and complete native receive results were verified, with no native ACK, credential leaks or surviving owned processes. This establishes one live exchange, not Runtime-enabled branch/reload/compaction, fsync admission, the native three-harness ring or rollout. A subsequent history-only test on copies used installed Pi RPC controls and real-model compaction: both source transcripts stayed unchanged, exact receive records remained stored across navigation/reload/restart/compaction, and active-context inclusion followed branch and compaction semantics. This does not establish renewed messaging authority or lossless summary context.

Subsequent [upgraded local Runtime proof](MCP-LIVE-PI.md#follow-up-upgraded-local-runtime-and-peer-replacement): two actual-model round trips passed around a typed read-only peer replacement. The successor received fresh authority; the retired credential returned `registration_stale`, while the controller's identical committed send retry recovered its original event without duplication or retargeting. A controller compaction overlapped the second exchange, with successful later retrieval in the unchanged namespace. Four events and their native tool results/references/receipts were verified without native ACK; the exact test peers were stopped while the controller and intentionally running Runtime service were preserved. This is not a same-session hot-reload/repair matrix, historical-token leak audit, fsync guarantee or general rollout approval.

Subsequent [Runtime-enabled same-session lifecycle proof](MCP-LIVE-PI.md#follow-up-runtime-enabled-same-session-lifecycle): five exchanges and ten events passed across baseline, reload, navigation away/restoration and normal-model compaction. Initiating pings were actual stdio MCP driver calls, not model-generated sends; both Pi models performed native retrieval/reply/receipt. Reload preserved the Pi process/session but renewed its namespace, rejected the old credential and preserved the original operation retry. The old MCP child and final owned resources were checked quiescent; exact native records, branch/context boundaries and credential scans passed. This closes the successful-transition slice, not failure-repair, fsync or native-ring gates.

## Historical evidence

[Stage 0](MCP-STAGE0.md) records actual restricted Claude/Codex compatibility and negative permission probes. Stage 1 recorded actual MCP publications Claude → Pi (`evt_161fb342-2f8a-438a-963d-e0ba194317b2`) and Codex → Claude (`evt_75cec17b-4d4b-429a-8aa5-0ceed8bd7efd`), both pending—not receive/admission evidence. Owned proof processes were stopped before copied auth removal.

`c9eb96b` added the real Pi adapter; `806220d` updated SDKs to 0.85.1. The version-refresh baseline passed 505 tests/49 files plus three installed-Pi cases. Prior compatibility plans are superseded by the user's no-legacy decision, not retroactively relabeled as completed work.

References: [Runtime contract](PROTOCOL.md), [workspace safeguards](WORKSPACES.md), MCP [lifecycle](https://modelcontextprotocol.io/specification/2025-11-25/basic/lifecycle), [stdio](https://modelcontextprotocol.io/specification/2025-11-25/basic/transports), [tools](https://modelcontextprotocol.io/specification/2025-11-25/server/tools).
